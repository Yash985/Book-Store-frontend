import React from "react";

const NoPage = () => {
  return (
    <div className="absolute top-[50%] left-[50%] transform -translate-x-[50%] -translate-y-[50%] text-5xl">
      Sorry We ran out of code
    </div>
  );
};

export default NoPage;
